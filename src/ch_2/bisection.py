# from ...lib.input_manager import InputManager

# bisectionInputManager = InputManager()

# def bisection_method(self, function = None, lowerLimit = None, upperLimit = None):
# 	bisectionInputManager.setFunc(function)
# 	bisectionInputManager.setLimits(lowerLimit, upperLimit)

# 	x = (bisectionInputManager.getLowerLimit() + bisectionInputManager.getUpperLimit()) / 2

# 	while abs(bisectionInputManager.getUpperLimit() - bisectionInputManager.getLowerLimit()) > 10e-6:
# 		if (bisectionInputManager.sympyFunction.subs())

# 	return 0
